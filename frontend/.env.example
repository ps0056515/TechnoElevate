# API base URL for browser requests. Leave unset for local dev — Vite proxies /api to the backend.
# When the SPA is served without that proxy (e.g. static files on another host/port), set the backend origin:
# VITE_API_URL=http://103.182.211.219:6000
VITE_API_URL=
